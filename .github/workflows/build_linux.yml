name: Linux Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Download package
        run: |
          wget -O archive.tar.zst https://mirror.msys2.org/mingw/mingw64/mingw-w64-x86_64-libwinpthread-13.0.0.r271.g937a01534-1-any.pkg.tar.zst
          
      - name: Unzip file
        run: |
          tar --zstd -xvf archive.tar.zst
          
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Windows-artifact
          path: ${{github.workspace}}
          if-no-files-found: ignore
          retention-days: 0
